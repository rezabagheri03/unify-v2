'use client';

// Expert-level approvals — same UI, just different role nav.
// The backend /pending endpoint automatically filters by role & department.

export { default } from '../../professor/approvals/page';
